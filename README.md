# dotfiles

Installs Ansible in a Python3 virtualenv then deploys my config (mostly just apt, services, and dotfiles)

KDE is an awesome desktop environment but trying to programmatically configure it SUCKS
