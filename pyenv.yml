---

- name: pyenv
  hosts: 127.0.0.1
  connection: local
  become: yes
  become_user: davido
  tasks:

  - name: install pyenv
    shell: curl https://pyenv.run | bash
    args:
      executable: /bin/bash
